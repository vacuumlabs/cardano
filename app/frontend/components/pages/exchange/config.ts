import {Currency} from '../../../exchange/types'

export const DEFAULT_SELL_CURRENCIES = ['btc' as Currency, 'eth' as Currency, 'ltc' as Currency]

export const DEFAULT_BUY_CURRENCIES = [
  'ada' as Currency,
  'btc' as Currency,
  'eth' as Currency,
  'ltc' as Currency,
]
